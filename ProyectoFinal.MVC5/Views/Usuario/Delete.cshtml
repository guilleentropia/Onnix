@model ProyectoFinal.MVC5.ViewModels.UsuarioViewModel

@{
    
    if (Session["UserId"] == "sanjuan")
    {
        Layout = "~/Views/Shared/_OtroLayout.cshtml";
    }

    if (Session["UserId"] == "mendoza")
    {
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

    if (Session["UserId"] == null)
    {
        Response.Redirect("~/Account/Login");
    }
    
    
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3 class="alert-danger">Are you sure you want to delete this?</h3>
<div>
    <h4>UsuarioViewModel</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NombreUsuario)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NombreUsuario, new { @class = "k-textbox" })
        </dd>
        

        <dt>
            @Html.DisplayName("Empresa")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EmpresaUsuario.Descripcion, new { @class = "k-textbox" })
        </dd>



        <dt>
            @Html.DisplayName("Perfil")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PerfilUsuario.Descripcion, new { @class = "k-textbox" })
        </dd>

        <dt>
            @Html.DisplayName("Empleado")
        </dt>

        <dd>
           @Html.DisplayFor(model => model.EmpleadoUsuario.Nombre, new { @class = "k-textbox" })
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
           @(Html.Kendo().Button()
                 .Name("imageButton")
                 .Content("Eliminar"))
            @Html.ActionLink("Volver al Listado", "Index", null, new { @class = "k-button" })
        </div>
    }
</div>
